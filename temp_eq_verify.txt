A tracer breakthrough curve carries more information than a single concentration value. Two processes contribute to the shape observed at the wellhead. The first is straightforward: tracer that accumulated in the proppant pack during shut-in is swept out when the well opens, producing a pulse that rises and falls as the slug passes the sampling point. The second is slower and less obvious: residual tracer continues to diffuse out of the epoxy matrix long after the main slug has passed, feeding a sustained, low-level signal into the produced oil. These two contributions overlap in time, and neither can be isolated by simply reading the concentration curve.

We separate them by modeling the transport from first principles. The one-dimensional advection-dispersion equation, ∂C/∂t + v ∂C/∂x = D ∂²C/∂x², governs the evolution of tracer concentration along the production tubing, with v = 4Q/(πd²) and D = α v. For the shut-in slug the classical instantaneous-injection solution [29] gives a Gaussian pulse: C_rise = (M/A)/sqrt(4π D t) exp[-(x - v t)² / (4 D t)]. For the sustained release the continuous-source solution gives an erfc form: C_fall = (C0/2) erfc[(x - v t) / sqrt(4 D t)]. We blend the two with a tanh weight, w(t) = 1/2 [1 + tanh((t - t0)/σ)], which shifts smoothly from the pulse-dominated early period to the diffusion-dominated tail. The full model, C(t) = cb + w(t) C_rise + [1 - w(t)] C_fall, is Eq. (2). The parameter σ is fixed at the sampling interval (4 min); the remaining six parameters are discussed below.